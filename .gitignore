# local working files and agent workflow scaffolding - never upstream
# docs/ is scratch in this repo: no branch has ever tracked a file under it.
docs/
.superpowers/

# clasp deployment config - both are per-spreadsheet and never upstream.
# .clasp.json holds a personal Apps Script project ID; appsscript.json is that
# project's own manifest (timezone, runtime), pulled down by clasp.
.clasp.json
.clasp.json.*
appsscript.json

# node
node_modules/
